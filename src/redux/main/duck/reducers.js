const INITIAL_STATE = {
    message: 'Działa!',
  };
  
  const mainReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
        return state;
    }
  };
  
  export default mainReducer;